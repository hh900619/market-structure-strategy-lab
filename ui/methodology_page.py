import streamlit as st


def render_methodology_page():
    st.markdown("# Methodology / About")
    st.caption("這一頁介紹這個系統的研究方法、核心架構，以及我如何用價格結構來分類市場。")

    # =====================================================
    # 1. 專案目的
    # =====================================================
    st.markdown("## 1. 專案目的")
    st.markdown(
        """
這個專案的核心，不是單純對價格套用某個技術指標，  
而是先回答一個更前面的問題：

**現在市場處於什麼樣的結構環境？**

我希望研究的是：

- 市場結構如何影響策略表現
- 同一個策略在不同市場背景下是否合理
- 原始策略訊號，是否應該再經過一層市場背景審核

因此，這個系統不是把所有價格資料一視同仁地回測，  
而是先分類市場，再研究策略與市場之間的相容性。
"""
    )

    # =====================================================
    # 2. 系統核心架構
    # =====================================================
    st.markdown("## 2. 系統核心架構")
    st.markdown(
        """
目前系統主要分成四層：

### (1) Market State Builder
先從價格資料中抽取市場狀態，建立市場結構標記。

### (2) Strategy Layer
策略本身只負責產生原始進出場訊號。  
目前版本包含：
- Trend Breakout
- Trend Pullback

### (3) Engine Layer
Engine 不負責發明策略，而是負責判斷：
**這個原始策略訊號，在目前市場背景下是否合理。**

它可以做的事包括：
- 允許進場
- 保守允許進場
- 降低持倉強度
- 提早出場
- 阻擋進場

### (4) Strategy Lab
用來研究：
- 原始策略交易圖
- 加入 Engine 後的交易圖
- 被 Engine 擋掉的交易
- 回測績效
- 交易明細
"""
    )

    # =====================================================
    # 3. 我如何判斷市場偏多或偏空
    # =====================================================
    st.markdown("## 3. 我如何判斷市場偏多或偏空")
    st.markdown(
        """
我不是直接用單一技術指標去定義市場偏多或偏空，  
而是先把「市場結構」拆成幾個可以量化的部分，再綜合判斷目前更接近哪一種狀態。

### 我看的不是單一答案，而是一組價格結構特徵
例如：

- 價格相對均線的位置
- 均線本身的斜率
- 最近高點與低點是否持續抬高或降低
- 區間寬度是否正在縮小
- 波動是否具有方向一致性
- 是否出現高波動但沒有延續的混亂行為

### 偏多不是主觀貼標
當多個結構特徵同時顯示：

- 價格站在較有利的位置
- 結構方向偏上
- 高點與低點呈現較健康的上移
- 沒有太強的混亂訊號

那市場就更接近**偏多結構**。

### 偏空也是同樣的概念
如果多個結構特徵同時顯示：

- 價格位置偏弱
- 結構方向偏下
- 近期低點持續下移
- 波動方向偏空

那市場就更接近**偏空結構**。

### 如果方向不一致
若市場沒有明確方向，價格反覆回到中間區域，  
或區間縮小、方向不延續，  
那系統就更傾向把它判斷成：

- 中性盤整
- 壓縮中性
- 高波動混亂

也就是說，我不是直接預測未來漲跌，  
而是在量化目前價格行為**更像哪一種市場狀態**。
"""
    )

    # =====================================================
    # 4. 目前策略
    # =====================================================
    st.markdown("## 4. 目前策略")
    st.markdown(
        """
目前版本先收斂在兩個策略：

### Trend Breakout
這是一個趨勢發動型策略。  
它的目標不是抄底或摸頭，而是在價格突破重要結構之後，參與可能的方向延伸。

### Trend Pullback
這是一個趨勢中繼型策略。  
它不想追最亢奮的突破，而是等待趨勢中的回踩，等到價格重新轉強後再順勢進場。

這兩個策略目前是我認為在這套市場結構框架下，最自然、最成熟的兩種策略。
"""
    )

    # =====================================================
    # 5. Engine 的角色
    # =====================================================
    st.markdown("## 5. Engine 的角色")
    st.markdown(
        """
Engine 的存在，是這個系統和一般單純回測最大的差別之一。

### 原始策略訊號 != 最終保留的交易
策略本身只回答：
- 這裡有沒有進場訊號？

但 Engine 會再問一次：
- 這個訊號在目前市場背景下，真的合理嗎？

例如：
- breakout 訊號可能出現在偏空背景中
- pullback 訊號可能出現在結構已經轉弱的地方
- 原始策略雖然給出訊號，但市場背景不一定支持

所以 Engine 的功能是：
- 過濾掉背景不支持的交易
- 在持倉期間根據背景變化做調整
- 協助我研究策略與市場背景的相容性

這也就是為什麼在 Strategy Lab 中，我會同時保留：
- 原始策略交易
- Engine 過濾後交易
- 被 Engine 擋掉的交易
"""
    )

    # =====================================================
    # 6. 目前版本範圍與限制
    # =====================================================
    st.markdown("## 6. 目前版本範圍與限制")
    st.markdown(
        """
目前版本的定位是：

**一個以市場結構分類為核心的策略研究與展示系統。**

它目前已經可以做到：
- 市場結構標記
- Breakout / Pullback 策略研究
- Engine-based 訊號審核
- 原始交易與 Engine 交易對照
- 互動式圖表與交易明細展示

但它目前仍然不是最終的 production trading system。  
例如：

- 目前仍以研究與展示為主
- 策略家族數量仍有限
- 多資產驗證與更嚴格統計檢定仍可持續加強
- 成本、滑價、走勢穩定性等還能再進一步細化

### 這個專案目前最重要的價值
不在於「找出一條萬能策略」，  
而在於建立一套更有邏輯的研究流程：

**先判斷市場，再研究策略是否適合這個市場。**
"""
    )

    st.markdown("---")
    st.caption(
        "Current version focus: market structure classification, trend-following strategies, and engine-based signal filtering."
    )